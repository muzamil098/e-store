/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ["fakestoreapi.com", "img.daisyui.com", "images.unsplash.com"],
  },
};

export default nextConfig;
